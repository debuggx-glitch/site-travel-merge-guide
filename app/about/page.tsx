import type {Metadata} from "next";export const metadata:Metadata={alternates:{canonical:"/about/"}};export default function Page(){return <main className="article"><p className="kicker">MERGE ROUTE</p><h1>How these guides are checked</h1><p className="dek">Every page separates official feature descriptions from live-client observations. Missing numbers remain unknown, screenshots stay linked to their source, and version-sensitive claims carry a checked date.</p></main>}
