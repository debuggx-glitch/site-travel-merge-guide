import type {Metadata} from "next";export const metadata:Metadata={alternates:{canonical:"/contact/"}};export default function Page(){return <main className="article"><p className="kicker">MERGE ROUTE</p><h1>Corrections</h1><p className="dek">When reporting a correction, include the exact URL, game platform, current version or event label, and a screenshot showing the result. A reproducible correction replaces a guess.</p></main>}
