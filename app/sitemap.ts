import type {MetadataRoute} from "next";import {guides} from "@/lib/guides";import {site} from "@/lib/site";export const dynamic="force-static";export default function sitemap():MetadataRoute.Sitemap{return ["","/about/","/privacy/","/contact/",...guides.map((g)=>`/guides/${g.slug}/`)].map((path)=>({url:`${site.url}${path}`,lastModified:"2026-09-04",changeFrequency:"weekly",priority:path?0.8:1}))}
